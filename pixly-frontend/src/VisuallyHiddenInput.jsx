import React from "react";
import { Input } from "@mui/material";

const HiddenInput = () => {
  return (
    <Input
      style={{
        display: "none",
      }}
    />
  );
};

export default HiddenInput;